import * as React from 'react';
import { View, Text, SafeAreaView } from 'react-native';

export default function SeekARide({ route, navigation }) {

    return (
        <Text>Seek A Ride</Text>
    )
}