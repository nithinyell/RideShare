
import { ActivityIndicator, FlatList, Text, View, StyleSheet, Image, ImageBackground, Platform } from 'react-native';

export default {
    COLOR: 'tomato',
    FONTFAMILY: Platform.OS === 'ios' ? 'Chalkboard SE' : 'notoserif',
}